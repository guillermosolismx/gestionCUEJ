// Importar el ModuleManager
import { moduleManager } from './modules.js';

// ...existing code...

// Ejemplo de uso:
function createNewScheduleModule(startDate, endDate) {
    return moduleManager.createModule(startDate, endDate);
}

// ...existing code...
